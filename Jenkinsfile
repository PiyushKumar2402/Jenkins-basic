pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'pwd'
            }
        }

        stage('Test') {
            steps {
                echo 'Testing application'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying application'
            }
        }
    }
}
